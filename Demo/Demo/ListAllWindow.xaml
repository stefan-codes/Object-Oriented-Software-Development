<Window x:Class="Demo.ListAllWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Demo"
        mc:Ignorable="d"
        Title="ListAllWindow" Height="359.209" Width="586.128">
    <Grid>
        <DataGrid x:Name="dataGrid" AutoGenerateColumns="False" Loaded="dataGrid_Loaded" ColumnWidth="*">
            <DataGrid.Columns>
                <DataGridTextColumn Header="MatricNo" Binding="{Binding Matric}" IsReadOnly="True"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Date of Birth" Binding="{Binding DateOfBirth, StringFormat=\{0:dd/MM/yyyy\}}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Coursework" Binding="{Binding CwMark}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Exam" Binding="{Binding ExamMark}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Mark" Binding="{Binding Mark}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
